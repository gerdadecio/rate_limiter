version: '3'
services:
  app:
    build: .
    command: rails server -p 3000 -b '0.0.0.0'
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    links:
      - redis
    environment:
      - REDIS_URL=redis://redis:6379
  redis:
    image: redis
    ports:
      - "6379:6379"


